services:
  riot-match-analytics-api:
    build:
      context: ./api
    container_name: riot-match-analytics-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
